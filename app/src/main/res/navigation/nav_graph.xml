<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/permissionScreen">

    <fragment
        android:id="@+id/permissionScreen"
        android:name="uz.unidev.ardemo.PermissionScreen"
        android:label="PermissionScreen">
        <action
            android:id="@+id/action_permissionScreen_to_ARScreen"
            app:destination="@id/ARScreen" />
    </fragment>

    <fragment
        android:id="@+id/ARScreen"
        android:name="uz.unidev.ardemo.ARScreen"
        android:label="ARScreen" />
</navigation>